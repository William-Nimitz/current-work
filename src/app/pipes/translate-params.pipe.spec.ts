import { TranslateParamsPipe } from './translate-params.pipe';

describe('TranslateParamsPipe', () => {
  it('create an instance', () => {
    const pipe = new TranslateParamsPipe();
    expect(pipe).toBeTruthy();
  });
});
