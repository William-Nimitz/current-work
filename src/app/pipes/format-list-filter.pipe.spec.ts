import { FormatListFilterPipe } from './format-list-filter.pipe';

describe('FormatListFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatListFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
