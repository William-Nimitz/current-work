import { ConversationVersion } from './conversation-version';

describe('ConversationVersion', () => {
  it('should create an instance', () => {
    expect(new ConversationVersion()).toBeTruthy();
  });
});
