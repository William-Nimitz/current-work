import { CreationFormatType } from './creation-format-type';

describe('CreationFormatType', () => {
  it('should create an instance', () => {
    expect(new CreationFormatType()).toBeTruthy();
  });
});
