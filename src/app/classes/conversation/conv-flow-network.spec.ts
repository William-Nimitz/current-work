import { ConvFlowNetwork } from './conv-flow-network';
const arg1 = null;
describe('ConvFlowNetwork', () => {
  it('should create an instance', () => {
    expect(new ConvFlowNetwork(arg1)).toBeTruthy();
  });
});
